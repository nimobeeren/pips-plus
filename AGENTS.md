- Use the Playwright CLI skill when testing in the browser.
- Prefer shadcn/ui components over making your own. Use the shadcn CLI to find and add new components when needed.
- Only use `pnpm`, never `npm`.
