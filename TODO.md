now:

- remove show solution button
- unlock puzzle editor only after completing all puzzles
- weird region IDs when creating a lot of them -> can we remove them? otherwise just use a number and don't show them to the user. remove domino ids too if possible
- regions have different colors after clicking play from editor
- can't rotate dominoes more than 90 degrees in editor
- navigating to /editor directly on deployed version yields 404

later:

- hard puzzle + dominoes don't fit on screen
- auto-fill generates invalid domino configurations
- cell background bug for some shapes with holes
